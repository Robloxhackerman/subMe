import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.robloxhackerman.subme',
  appName: 'sub-Me',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
